apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lora-adapters-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  # Uncomment and modify the storageClassName if you need a specific storage class
  # storageClassName: standard

